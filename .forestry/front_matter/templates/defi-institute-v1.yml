---
label: DeFi Institute v1
hide_body: false
fields:
- name: hero_image
  type: file
  config:
    maxSize: 64
  label: Hero Image
  description: This image is for the hero image on the front page.
  hidden: false
- name: blocks
  type: blocks
  label: Blocks
  template_types: []
  config:
    min: 
    max: 
  description: These are page blocks for creating front page sites/content
